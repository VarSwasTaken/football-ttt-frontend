import {Header, Footer} from '../components/index';

const Home = () => {
    return (
        <div class="container">
            <Header />
            <Footer />
        </div>
    );
};

export default Home;
