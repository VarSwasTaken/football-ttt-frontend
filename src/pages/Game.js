import {Header, Footer} from '../components/index';

const Game = () => {
    return (
        <div class="container">
            <Header />
            <section className="content"></section>
            <Footer />
        </div>
    );
};

export default Game;
